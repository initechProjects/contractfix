angular.module('app.forgotPassword', [])


.controller('forgotPasswordController', function ($scope, $rootScope, $window, $location, Dashboard) {
	console.log("I am inside forgotpasswordCtrl", $rootScope.token);

    
});