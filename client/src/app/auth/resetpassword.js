angular.module('app.resetPassword', [])


.controller('resetPasswordController', function ($scope, $rootScope, $window, $location, Dashboard) {
	console.log("I am inside resetpasswordCtrl", $rootScope.token);

    
});